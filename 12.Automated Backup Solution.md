# Automated Backup Solution
## Question: 
Write a script to automate the backup of specified directory to a remote server or cloud storage solution. The script should provide a report on the success or failure of the backup operation.

## Answer: 
Here's a straightforward shell script to automate the backup of a specified directory to a remote server using 'rsync'. The script also generates a report on the success or failure of the backup operation.
